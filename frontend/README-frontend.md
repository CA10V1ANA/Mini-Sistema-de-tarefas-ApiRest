# Frontend (Vite + React)

Este diretório contém um app React minimal criado para se comunicar com o backend Grails.

Run (na raiz `frontend`):

```cmd
npm install
npm run dev -- --port 3000
```

O app espera a API no `http://localhost:8080/api/tarefas`.
